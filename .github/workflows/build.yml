name: Nightly pre-release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-win:
    runs-on: windows-latest
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x  # 改成 8

      - name: Restore (default)
        run: dotnet restore

      - name: Restore (win-x64)
        run: dotnet restore -r win-x64

      - name: Build (Release)
        run: dotnet build -c Release --no-restore

      # 不带运行时
      - name: Publish framework-dependent
        run: >
          dotnet publish Verviewer.csproj -c Release
          -p:DebugType=None
          -p:DebugSymbols=false
          -o out/win-fdd
          --no-restore

      # 带运行时 + 压缩
      - name: Publish self-contained single-file
        run: >
          dotnet publish Verviewer.csproj -c Release
          -r win-x64 --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -p:EnableCompressionInSingleFile=true
          -p:DebugType=None
          -p:DebugSymbols=false
          -o out/win-scd
          --no-restore

      - name: Zip outputs
        run: |
          powershell Compress-Archive -Path out\win-scd\* -DestinationPath out\Verviewer-win-x64-selfcontained.zip
          powershell Compress-Archive -Path out\win-fdd\* -DestinationPath out\Verviewer-win-framework-dependent.zip

      - name: Delete old release
        uses: cb80/delrel@latest
        with:
          tag: nightly
          token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Create GitHub pre-release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: nightly
          name: Nightly Build (Auto Updated)
          body: |
            ## 📦 下载说明
            
            ### ⭐ 推荐下载（大多数用户选这个）
            
            👉 **[点击下载 Verviewer-win-x64-selfcontained.zip](https://github.com/${{ github.repository }}/releases/download/nightly/Verviewer-win-x64-selfcontained.zip)**
            
            解压后双击 `Verviewer.exe` 即可运行，无需安装任何东西！
            
            ---
            
            ### 💾 精简版（体积更小）
            
            👉 [点击下载 Verviewer-win-framework-dependent.zip](https://github.com/${{ github.repository }}/releases/download/nightly/Verviewer-win-framework-dependent.zip)
            
            需要先安装 [.NET 8 运行时](https://dotnet.microsoft.com/download/dotnet/8.0)，适合已安装 .NET 的用户。
            
            ---
            
            ⚠️ 这是自动构建的测试版本，可能不稳定。
            
            | 信息 | 详情 |
            |------|------|
            | 🕐 构建时间 | ${{ github.event.head_commit.timestamp }} |
            | 📝 提交 | [`${{ github.sha }}`](https://github.com/${{ github.repository }}/commit/${{ github.sha }}) |
          prerelease: true
          draft: false
          files: |
            out/Verviewer-win-x64-selfcontained.zip
            out/Verviewer-win-framework-dependent.zip